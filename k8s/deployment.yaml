apiVersion: apps/v1
kind: Deployment
metadata:
  name: netflix-ui
  namespace: netflix
spec:
  replicas: 2
  selector:
    matchLabels:
      app: netflix-ui
  template:
    metadata:
      labels:
        app: netflix-ui
    spec:
      containers:
        - name: netflix-ui
          image: atharv2430/netflix:1
          ports:
            - containerPort: 3000
